# ============================================================
# Cross-platform line ending normalization
# Ensures consistent LF in repo, native line endings in working copy
# Prevents "file changed" diffs caused by CRLF vs LF differences
# between Windows and macOS/Linux
# ============================================================

# Default: auto-detect text files → normalize to LF in repo
* text=auto eol=lf

# ── Source code ──────────────────────────────────────────────
*.js    text eol=lf
*.jsx   text eol=lf
*.ts    text eol=lf
*.tsx   text eol=lf
*.json  text eol=lf
*.css   text eol=lf
*.scss  text eol=lf
*.html  text eol=lf
*.md    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.xml   text eol=lf
*.svg   text eol=lf

# ── Config files ─────────────────────────────────────────────
*.env           text eol=lf
*.gitignore     text eol=lf
*.gitattributes text eol=lf
*.editorconfig  text eol=lf
*.npmrc         text eol=lf
*.nvmrc         text eol=lf
Dockerfile      text eol=lf
Makefile        text eol=lf

# ── Shell scripts ────────────────────────────────────────────
*.sh   text eol=lf
*.bash text eol=lf

# ── Windows scripts (keep CRLF) ─────────────────────────────
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# ── Binary files (no text conversion) ───────────────────────
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
*.ttf  binary
*.eot  binary
*.mp3  binary
*.mp4  binary
*.webm binary
*.pdf  binary
*.zip  binary
*.tar.gz binary
*.exe  binary
*.node binary

# ── Lock files (exact byte-for-byte match required) ─────────
package-lock.json text eol=lf -diff
yarn.lock         text eol=lf -diff
pnpm-lock.yaml    text eol=lf -diff
